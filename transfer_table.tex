\begin{table}[t]
    \centering
    \scriptsize
    \setlength{\tabcolsep}{3.6pt}

    \resizebox{\columnwidth}{!}{%
    \begin{tabular}{lccc ccc}
        \toprule
        & \multicolumn{3}{c}{\textbf{Avg.\ edited accuracy (\%)}}
        & \multicolumn{3}{c}{\textbf{Avg.\ disagreement with twin (\%)}} \\
        \cmidrule(lr){2-4}
        \cmidrule(lr){5-7}

        \textbf{Source}
        & \textbf{PIQA}
        & \textbf{HSwag}
        & \textbf{WG}
        & \textbf{PIQA}
        & \textbf{HSwag}
        & \textbf{WG} \\
        \midrule

        Twin (target) & 80.4 & 61.4 & 83.7 & --- & --- & --- \\
        Naive         & 89.9 & 78.6 & 90.5 & 14.8 & 23.6 & 18.2 \\
        \midrule
        PIQA          & 84.3 & 65.4 & 88.2 & \textbf{7.5} & 8.5 & 14.6 \\
        HellaSwag     & 84.3 & 64.9 & 87.5 & 7.9 & \textbf{8.1} & 14.7 \\
        WinoGrande    & 84.0 & 62.0 & 85.5 & 7.7 & 8.2 & \textbf{11.7} \\
        Wikipedia     & 84.1 & 63.7 & 86.9 & 7.8 & 8.2 & 13.7 \\
        \bottomrule
    \end{tabular}%
    }

    \caption{Cross benchmark transfer. Rows are the corpus the edit direction was fit on, columns
    the benchmark it was applied to; \emph{Naive} is the contaminated checkpoint with no edit and
    \emph{Twin} is the uncontaminated model the edit is trying to recover. Every cell averages the
    six duplication levels $\{0,1,4,16,64,256\}$, each counted once. The step is chosen per cell as
    the one minimising disagreement at $\text{dup}=0$, items neither model was trained on, so no
    cell is selected on a contaminated row. Disagreement is the headline metric: it is the share of
    test items the edited model answers differently from the twin, and unlike accuracy it cannot be
    lowered by damaging the model. Bold marks the best source per benchmark. Accuracy is left
    unbolded because averaging signed gaps lets an overshoot at one duplication level pay for an
    undershoot at another; WinoGrande $\rightarrow$ HellaSwag is the one cell where this happens.}
    \label{fig:cross_benchmark_transfer}
\end{table}
